# Core pipeline (fetch + translate + heuristic scoring) has NO third-party
# dependencies — it uses only the Python standard library (urllib, xml, json)
# and runs on Python 3.8+.
#
# Optional: LLM-based quality review via the Cursor SDK. Only needed if you set
# CURSOR_API_KEY to enable LLM scoring. Requires Python 3.10+.
cursor-sdk ; python_version >= "3.10"
